import 'package:flutter/material.dart';

class MobileScreenLayhout extends StatelessWidget {
  const MobileScreenLayhout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('This is Mobile')),
    );
  }
}
